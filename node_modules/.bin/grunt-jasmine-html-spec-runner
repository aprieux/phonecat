#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-jasmine-html-spec-runner/bin/grunt-jasmine-html-spec-runner" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-jasmine-html-spec-runner/bin/grunt-jasmine-html-spec-runner" "$@"
  ret=$?
fi
exit $ret
